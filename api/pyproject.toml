[project]
name = "bramble-api"
version = "0.1.0"
description = "REST API for Bramble LoRa irrigation system"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "pyserial>=3.5",
    "duckdb>=0.10.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.4.0",
]
